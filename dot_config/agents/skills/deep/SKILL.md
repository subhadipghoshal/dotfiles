---
name: deep
description: "Raise this turn's reasoning effort to xhigh for a genuinely hard sub-problem: tricky debugging, a design tradeoff, an adversarial review pass. Costs more — reach for it deliberately, not by default."
effort: xhigh
---

# Deep

This turn runs at elevated effort. Load `complexity-assessment` first if sizing the problem is
itself part of the work — it already carries the right rubric (scope, uncertainty, coupling,
risk, verification effort) and there is no reason to re-derive it here.

Use the budget on the actual hard part: reasoning through tradeoffs, chasing a failure mode to
its root cause, checking work adversarially. Do not spend it padding an answer that was already
clear at the baseline.
