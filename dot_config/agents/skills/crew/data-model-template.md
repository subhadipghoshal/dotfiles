# Data model template

1. **Baseline restated** from `plan.md`, so the file stands alone for a cold reader.
2. **Entities and ownership**.
3. **Keys, constraints, invariants**.
4. **Access paths and indexes**.
5. **Migration, backfill, rollback**.
6. **Compatibility** for existing readers and writers.
7. **Concurrency and isolation semantics**.
8. **Amendments required** to `plan.md`, or `none`.
